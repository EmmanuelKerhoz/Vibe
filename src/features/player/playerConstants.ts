/** Spotify brand green — used across multiple player sub-components. */
export const SPOTIFY_GREEN = '#1DB954';

/** Tinted box backgrounds for various player sections. */
export const LCARS_BOX_COLORS: readonly string[] = [
  'rgba(255,153,0,0.18)',
  'rgba(153,102,204,0.18)',
  'rgba(204,153,102,0.18)',
  'rgba(255,102,102,0.18)',
  'rgba(102,204,255,0.18)',
];

/** Default aspect ratio for the video player before metadata loads. */
export const DEFAULT_VIDEO_ASPECT_RATIO = 16 / 9;

/** Maximum number of tracks the local library can hold. */
export const LIBRARY_CAPACITY = 50;
