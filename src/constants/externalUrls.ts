/**
 * External URL constants.
 *
 * Centralised so that URL schemes can be updated in a single place.
 */

/**
 * SUNO web creation URL template.
 * Append `?prompt={encodeURIComponent(musicalPrompt)}` when opening.
 * Update here if SUNO's URL scheme changes.
 */
export const SUNO_CREATE_URL = 'https://suno.com/create';
