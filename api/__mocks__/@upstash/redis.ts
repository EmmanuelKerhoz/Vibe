/**
 * Manual stub for @upstash/redis.
 * Vitest resolves manual mocks relative to the file under test.
 * Place: api/__mocks__/@upstash/redis.ts
 */
export class Redis {
  constructor(_opts: { url: string; token: string }) {}
}
